
spring:
  application:
    name: retelling-bot
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    hikari:
      schema: retelling_bot
      connection-init-sql: CREATE SCHEMA IF NOT EXISTS ${spring.datasource.hikari.schema};
  liquibase:
    enabled: true
    change-log: ./db/changelog/changelog-master.yml
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.0
          max-tokens: 8192

retelling:
  executors:
    tg-listener:
      threads: 10
      queue-capacity: 1000
  scheduled-jobs:
    source-parsing:
      habr-cron: 0 0 * * * *
    event-processing:
      cron: 0 * * * * *
  publishing:
    chat-id: ${PUBLISHING_CHAT_ID}
    java-retelling-tg-topic-id: ${JAVA_RETELLING_TG_TOPIC_ID}
    rate-tg-topic-id: ${RATE_TG_TOPIC_ID}
  rate:
    threshold: 70 # Оценка материала должна быть выше 70% для его публикации
  data-finder:
    habr:
      period-of-days-to-look-for: 1

  prompt:
    retelling: ${PROMPT_RETELLING}
    rate-material: ${PROMPT_RATE_MATERIAL}

bot:
  name: ${BOT_NAME}
  token: ${BOT_TOKEN}

logging:
  level:
    root: INFO
  console:
    enabled: true